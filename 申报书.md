# 2026 MoonBit Hackathon - 项目申报书（验收版）

## 1. 项目基本信息

- **项目名称**：moon-change-point (MoonBit 在线变点检测库)
- **项目方向**：MoonBit 生态库 / 算法库与工具链生态扩展
- **GitLink 仓库链接**：[https://gitlink.org.cn/sgacc/moon-change-point](https://gitlink.org.cn/sgacc/moon-change-point)
- **GitHub 仓库链接**：[https://github.com/Zy789kl/moon-change-point](https://github.com/Zy789kl/moon-change-point)
- **开源协议**：Apache-2.0
- **原创性声明**：本项目为原创 MoonBit 实现的统计过程控制与在线变点检测算法库，不引入未授权第三方源码；CI 使用 MoonBit 最新 stable 工具链。

---

## 2. 项目背景与目标

### 2.1 项目背景
在当今实时计算和流数据处理领域，快速响应指标异常是系统稳定性和业务敏捷性的核心要求。然而，传统的阈值告警往往会导致严重的“告警疲劳”（Alert Fatigue），并不能有效检测出缓慢漂移或者分布形态的变化。
目前在 MoonBit 语言生态中，主要侧重于核心数据结构、基础库以及 WebAssembly 生态的建设，但在数据科学、在线机器学习和工业级时间序列处理领域尚存在一定空白。本项目旨在填补 MoonBit 生态在“在线时间序列异常检测”方向的空白，提供一个轻量、高效、易用的变点检测算法库。

### 2.2 目标与价值
`moon-change-point` 致力于提供工业级的在线变点检测能力。由于 MoonBit 编译到 Wasm 后具有极小的体积和卓越的执行效率，非常适合边缘计算节点（Edge Computing）、IoT 设备的数据清洗与流处理分析。
本项目的核心价值在于：
1. **轻量极速**：得益于 MoonBit 和 Wasm，算法开销极低。
2. **多算法覆盖**：针对不同数据特征（均值漂移、突发尖刺、分布切换）提供不同的经典与贝叶斯检测算法。
3. **消除告警疲劳**：内置告警抑制逻辑，满足工业生产环境的直接需求。

---

## 3. 核心功能与技术实现

### 3.1 核心功能特性
1. **CUSUM (Cumulative Sum) 算法**
   - 实现经典的双向累积和控制图，能够极其敏锐地发现序列均值的微小漂移。
   - 支持动态 `drift`（漂移容忍度）和 `control_limit`（控制域）的灵活配置。
2. **Page-Hinkley 测试**
   - 提供具备遗忘因子（Forgetting Factor / Alpha）的增量均值更新。
   - 能够有效适应微小的白噪声并从局部波动中自愈。
3. **Bayesian Online Change Point Detection (贝叶斯在线变点检测)**
   - 使用 Normal-Gamma 共轭先验近似实现的流式贝叶斯变点推断。
   - 实时计算每一个时间步下各“当前游程长度”（Run Length）的后验概率。
   - 使用常数风险率（Constant Hazard Rate）模型预测变点爆发的可能性。
4. **Detector 统一封装与告警抑制**
   - 采用 MoonBit `enum` 构建统一的 `DetectorType` 分发器。
   - 原生内置 `suppression_limit`（告警抑制 / 降频）逻辑，检测到异常后的指定周期内进行静默，完美契合监控系统行为。
5. **验收阶段扩展**
   - 增加迟到数据重排、时间窗口聚合、离线分段、多变量 Mahalanobis/投影、回放比较器、阈值校准、解释性证据、SLO、事件聚类和告警路由。

### 3.2 架构与代码组织
项目严格遵循 MoonBit 推荐规范，采用标准的包管理结构，并且对每一个算法提供全覆盖的自动化测试验证：
- `cusum.mbt` / `cusum_test.mbt`：CUSUM 算法实现与黑盒测试。
- `page_hinkley.mbt` / `page_hinkley_test.mbt`：Page-Hinkley 算法实现与噪声恢复测试。
- `bayesian.mbt` / `bayesian_test.mbt`：流式贝叶斯推断实现与概率转移测试。
- `detector.mbt` / `detector_test.mbt`：统一包装器及测试。
- `moon.pkg` & `moon.mod`：基于 `moonbitlang/core` 标准库的严格依赖管理。
- `benchmark.mbt` / `BENCHMARK.md`：固定种子、固定 512 点场景和真实本机耗时记录。
- `.github/workflows/test.yml`：最新 stable MoonBit 的 Ubuntu/macOS/Windows 矩阵 CI，执行 fmt、check、build、info 和 all-target test。

---

## 4. 实施计划与里程碑

- **阶段一：基础算法构建（已完成）**
  - 完成 CUSUM 与 Page-Hinkley 算法的原型开发。
  - 完成 `moon fmt` 和 `moon check` 通过，确保类型安全。
- **阶段二：贝叶斯算法与统一抽象（已完成）**
  - 实现基于 Normal-Gamma 先验的简化 Bayesian 变点检测。
  - 引入 `Detector` 类型，增加生产环境必备的告警静默功能。
- **阶段三：工程化与开源合规（已完成）**
  - 补充完善包含代码示例的 `README.mbt.md`，实现文档即代码（DocTest）。
  - 创建 GitHub Actions 自动化测试流水线。
  - 推送代码至 GitLink 与 GitHub 平台，保证唯一真实贡献者。
- **阶段四：Hackathon 验收交付（已完成）**
  - 完成带时间戳的迟到数据重排、watermark、窗口聚合和流式 pipeline。
  - 完成多维时间序列检测、离线分段、回放复现、阈值学习与质量门禁。
  - 完成 SLO/事件/告警路由等实际应用侧能力，并提供可复现基准与 410 项边界/回归/集成测试。
- **阶段五：后续演进**
  - 面向真实业务数据集补充跨平台性能回归和更丰富的 dashboard/Canvas 适配。

---

## 5. 项目预期产出与验收标准

1. 包含上述算法及工程化扩展且实现源码超过 8,000 行的 `moon-change-point` 源码库。
2. Git 提交记录规范、有效，且全部由账号持有人主导提交。
3. 仓库配备多平台 CI，在 MoonBit 最新 stable 工具链下执行 `fmt`、`check`、`build`、`info` 和 all-target `test`。
4. 包含详尽的中英文使用文档和 License 声明。
5. 通过官方 `osc2026-guide` 建议的规模与健康度自查。

## 6. 验收实测记录

- 本地 MoonBit：`moon 0.1.20260807`，`moonc v0.10.7`。
- `moon check --deny-warn --target all`、`moon build --target all` 通过。
- `moon test --deny-warn`：410/410 通过；wasm 与 wasm-gc 通过。Windows 本机未安装 `node.exe`，因此 JS 测试由 CI 矩阵执行。
- 实现源码 8,600+ 行，全部 MoonBit 源码 12,000+ 行；基准数据和实际耗时见 `BENCHMARK.md`。
